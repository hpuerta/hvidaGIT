<%= form_for(@outlook, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @outlook.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@outlook.errors.count, "error") %> prohibited this outlook from being saved:</h4>

      <ul>
      <% @outlook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :correo, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :correo, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :clave, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :clave, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :nombre, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :nombre, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
